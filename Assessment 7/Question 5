#include <stdio.h>
void disp_sum(int,int);
int main()
{
   int a,b=0;
   printf("Enter the number : ");
   scanf("%d",&a);
   disp_sum(a,b);
}
void disp_sum(int a,int b)
{
    int i=1;
    while(i<=a)
    {
        b=b+i;
        i++;
    }
    printf("%d\n",b);
}
