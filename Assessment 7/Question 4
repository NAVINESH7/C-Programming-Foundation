#include <stdio.h>
void disp_descend(int);
int main()
{
   int a;
   printf("Enter the number : ");
   scanf("%d",&a);
   disp_descend(a);
}
void disp_descend(int a)
{
    int i=a;
    while(i>=1)
    {
        printf("%d\n",i);
        i--;
    }
}
