#include <stdio.h>
void disp_odd(int);
int main()
{
   int a;
   printf("Enter the number : ");
   scanf("%d",&a);
   disp_odd(a);
}
void disp_odd(int a)
{
    int i=1;
    while(i<=a)
    {
        printf("\n%d",i);
        i=i+2;
    }
    
}
