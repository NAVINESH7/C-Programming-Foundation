#include <stdio.h>
void disp_rsum(int,int);
int main()
{
   int a,b=0;
   printf("Enter the number : ");
   scanf("%d",&a);
   disp_rsum(a,b);
}
void disp_rsum(int a,int b)
{
    int i=a;
    while(i>=1)
    {
        b=b+i;
        i--;
    }
    printf("%d\n",b);
}
